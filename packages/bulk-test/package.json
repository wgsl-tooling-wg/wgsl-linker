{
  "name": "bulk-test",
  "version": "0.5.0-d1",
  "type": "module",
  "scripts": {
    "format": "prettier . --write",
    "lint": "eslint src",
    "organize": "organize-imports-cli tsconfig.json",
    "prepublishOnly": "run-s build",
    "test": "vitest",
    "bulk-test:once": "vitest run"
  },
  "dependencies": {
    "wgsl-linker": "workspace:*"
  }
}
