{
  "name": "bulk-test",
  "version": "0.5.0-d1",
  "type": "module",
  "scripts": {
    "format": "prettier . --write",
    "lint": "eslint src",
    "organize": "organize-imports-cli tsconfig.json",
    "test": "vitest",
    "bulk-test": "vitest run"
  },
  "dependencies": {
    "wgsl-linker": "workspace:*"
  },
  "devDependencies": {
    "@types/diff": "^5.0.9",
    "diff": "^5.2.0"
  }
}
